<div class="backdrop" aria-hidden="true"><div class="registration"></div><div class="field"></div></div>

<style>
	.backdrop { position: fixed; z-index: -1; inset: 0; overflow: hidden; pointer-events: none; background: var(--color-background); }
	.registration { position: absolute; left: max(24px, calc((100vw - 1320px)/2)); top: 0; bottom: 0; width: 1px; background: var(--color-border); }
	.field { position: absolute; width: 52vw; height: 52vw; right: -19vw; top: 14vh; background: radial-gradient(circle, rgba(7,84,63,.17), transparent 68%); }
</style>
